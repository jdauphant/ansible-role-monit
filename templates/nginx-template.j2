# {{ ansible_managed }}
check process nginx with pidfile /var/run/nginx.pid
  start program = "service nginx start"
  stop  program = "service nginx stop"
  group www
{% for k,v in item.server.iteritems() if k.find('host') != -1 %}
  if failed host {{ v.host }} port {{ v.port }}{% if v.port == 443 %}type tcpssl{% endif %} protocol http then restart
{% endfor %} 
  if 5 restarts within 5 cycles then timeout
