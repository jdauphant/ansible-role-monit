# {{ ansible_managed }}
check filesystem {{ item.service.name }} with path {{ item.service.path }}
    if space usage > 80% then alert
    if space usage > 85% then alert
    if space usage > 90% then alert
    if space usage > 95% then alert
